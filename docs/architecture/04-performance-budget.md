# 性能预算

## 总原则

性能优化必须服从玩法冻结原则，不能通过改节奏、改判定、改视觉密度口径来“伪提升”。

## 当前热点

- `Game.ts` 高频扫描与临时对象创建
- `render.ts` 每帧大量特效与绘制 pass 混杂
- 多处 `cellKey` / `Set<string>` / 数组扫描
- 快照与 UI 同步中的频繁拼装

## 后续优化方向

- 收敛为统一的数值索引与网格工具
- 降低每帧字符串 key 构造
- 复用高频特效对象
- 缩小需要重建的渲染层范围
- 保持现有 quality level 策略，但让它更清晰可测
