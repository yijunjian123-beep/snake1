# PRODUCT_SPEC

## 项目目标

《NEON SERPENT：霓虹吞星》是一款电脑和手机都能玩的单页创意贪吃蛇游戏。目标是在有限时间内交付一个稳定、直观、第一眼就有完整感的霓虹宇宙体验。

## 第一阶段目标

本阶段只建立可运行骨架：

- Vite + TypeScript 项目
- Canvas 2D 全屏渲染
- DOM UI 承载开始界面与基础状态
- DPR 与 resize 处理
- 清晰的主循环与模块边界

## 基础操作体验

基础操作规则现在统一为一套可理解、可感知的速度体验：

- 基础速度较当前版本下调 20%
- 按住与蛇头方向相同的输入时，进入加速态，速度提升 30%
- 按住与蛇头方向相反的输入时，进入减速态，速度降低 50%
- 反向输入只负责减速，不允许瞬间掉头
- Space 是加速别名，Shift 是减速别名，不会和方向输入叠加出第二层速度
- 速度变化只在状态切换时短暂出现在蛇头附近，约 1.5s 后渐隐
- 冲刺按钮保留为独立冲刺能力
- 键盘（WASD / 方向键）与移动端摇杆共享同一套规则

## 非目标

- 不做后端
- 不做账号系统
- 不做联网功能
- 不引入外部图片、字体、音频素材
- 不把基础操作体验做成复杂教程流程

## 架构约定

- `Game` 负责生命周期、输入分发、速度状态与 UI 同步
- `render` 负责 Canvas 画面、速度反馈和视觉表现
- `input` 负责把键盘与触摸输入映射成游戏动作
- `audio` 只提供浏览器原生音效入口
- `storage` 只保存本地最高分

## 后续玩法方向

- 黑洞障碍、星门传送、冲刺
- 粒子爆炸、震屏、背景脉冲
- 移动端虚拟摇杆与冲刺按钮的手感继续优化
