# PRODUCT_SPEC

## 产品目标

《NEON SERPENT：霓虹吞星》是一个电脑和手机都能玩的创意贪吃蛇单页游戏。目标是在有限时间内交付稳定、直观、第一眼有完成度的霓虹宇宙风格体验。

## 第一阶段目标

本阶段只建立可运行骨架：

- Vite + TypeScript 项目。
- Canvas 2D 全屏渲染。
- DOM UI 承载开始界面与基础状态。
- DPR 与 resize 处理。
- 主循环与模块边界清晰。

## 非目标

- 不实现完整蛇移动。
- 不实现星核、障碍、传送、碰撞或计分规则。
- 不引入外部图片、字体、音频、后端或账号能力。

## 架构约定

- `Game` 管理生命周期、阶段、输入分发和 UI 同步。
- `render` 只负责 Canvas 尺寸和画面绘制。
- `input` 只做物理输入到游戏动作的映射。
- `audio` 只提供浏览器原生音效入口。
- `storage` 只保存可序列化数据，例如最高分。

## 后续玩法方向

- 蛇移动、吃星核增长、得分、连击倍率。
- 黑洞障碍、星门传送、冲刺。
- 粒子爆炸、震屏、背景脉冲。
- 桌面键盘控制和移动端虚拟摇杆。

